package exceptions;

/**
 * Adventure Path Parser
 * 
 * <p>An adventurer discovers a map with encoded location data in a specific format: 
 * "location:distance:treasureCount". Parse this information and extract the location name.
 * The data might be malformed or incomplete, so handle various error cases gracefully.</p>
 * 
 * <p><b>Learning Focus:</b> Using multiple catch blocks to handle different types of exceptions
 * and providing appropriate error messages for each case.</p>
 */
public class AdventurePathParser {
    /**
     * Parses an adventure path string and extracts the location information.
     * 
     * @param path the path string in format "location:distance:treasureCount"
     * @return a formatted message with the location info, or an error message if parsing fails
     */
    public static String parsePath(String path) {
        // TODO: Use try-catch with multiple catch blocks
        throw new UnsupportedOperationException("Not yet implemented");
    }
}

