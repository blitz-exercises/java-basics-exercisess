package loops;

/**
 * Elevator Panel
 * 
 * <p>An elevator building has floors numbered 0 to 20. When users press buttons on the panel,
 * the system must validate that the requested floor is actually reachable. Invalid floor requests
 * (negative or beyond the top floor) should be rejected.</p>
 * 
 * <p><b>Learning Focus:</b> Using validation to check input boundaries and handle invalid cases.</p>
 */
public class ElevatorPanel {
    /**
     * Validates whether a floor number is accessible in the building.
     * 
     * @param floor the requested floor number
     * @return true if the floor is within the valid range (0-20 inclusive), false otherwise
     */
    public static boolean isValidFloor(int floor) {
        // TODO
        throw new UnsupportedOperationException("Not yet implemented");
    }
} 