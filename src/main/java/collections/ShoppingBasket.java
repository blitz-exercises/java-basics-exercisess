package collections;

import java.util.List;

/**
 * Shopping-Basket
 * 
 * <p>A customer fills their shopping basket with items while shopping at a store. To display
 * the receipt, the cashier needs to see all items in the basket as a readable list, with each
 * item separated by a comma.</p>
 * 
 * <p><b>Learning Focus:</b> Working with lists to store multiple items and combining them into a formatted string.</p>
 */
public class ShoppingBasket {
    /**
     * Formats a list of shopping items into a comma-separated string.
     * 
     * @param items a list of product names
     * @return the items formatted as a comma-separated string (e.g., "milk, bread, eggs")
     */
    public static String join(List<String> items) {
        // TODO
        throw new UnsupportedOperationException("Not yet implemented");
    }
}
 