package collections;

import java.util.Map;

/**
 * Vowel-Counter
 * 
 * <p>A linguistic analysis tool needs to count the frequency of vowels in a text sample.
 * For each vowel (a, e, i, o, u), track how many times it appears in the sentence,
 * ignoring case differences.</p>
 * 
 * <p><b>Learning Focus:</b> Using maps to accumulate counts and updating map values within a loop.</p>
 */
public class VowelCounter {
    /**
     * Counts the frequency of each vowel in a sentence.
     * 
     * @param sentence the text to analyze
     * @return a map where keys are vowel characters and values are their counts
     *         (includes vowels a, e, i, o, u in lowercase, case-insensitive matching)
     */
    public static Map<Character,Integer> count(String sentence) {
        // TODO
        throw new UnsupportedOperationException("Not yet implemented");
    }
} 